{
  "name": "waveboard-api",
  "version": "1.0.0",
  "description": "WaveBoard session key API — deployed on Vercel",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "mongodb": "^6.7.0"
  }
}
